// src/plugins/revalidate/server/register.ts

export default ({ strapi }: { strapi: any }) => {
    console.log('[Revalidate] Registering plugin...');

    // Plugin registration logic (if needed)
    // For example, you could register configuration validation here

    console.log('[Revalidate] Plugin registration complete');
};